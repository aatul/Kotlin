fun sum(x: Int, y: Int): Int {
    // some code
    return x + y
}

// Shortcut way of function implementation
fun multiply(x: Int, y: Int) = x * y

fun main() {
    println(sum(1, 2))
    println(multiply(2,5))
}